#include<bits/stdc++.h>
using namespace std;

int main(){
    cin.tie(0);
    ios::sync_with_stdio(false);

    string input;
    while(getline(cin, input)){
        cout << input << "\n";
    }

    return 0;
}