#include<bits/stdc++.h>
using namespace std;

int main(){
    cin.tie(0);
    ios::sync_with_stdio(false);

    string input;
    int n;
    cin >> input;
    cin >> n;
    cout << input[n-1];

    return 0;
}