for(int i = 0; i < n; i++){
    //     int input;
    //     cin >> input;
    //     scores.push_back(input);
    // }

    // sort(scores.begin(), scores.end(), greater<int>());

    // for(auto s : scores) cout << s << " ";