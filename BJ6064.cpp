#include<bits/stdc++.h>
using namespace std;

int n;

int main(){
    cin.tie(0);
    ios::sync_with_stdio(false);

    cin >> n;

    for(int i = 0; i < n; i++){
        int m, n, x, y = 0;
        cin >> m >> n >> x >> y;
        
    }

    return 0;
}